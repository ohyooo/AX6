name: Build Cudy ImmortalWrt

on:
  workflow_dispatch:
  push:

env:
  CCACHE_DIR: ${{ github.workspace }}/.ccache

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: |
            immortalwrt/dl
            ${{ github.workspace }}/.ccache
          key: ${{ runner.os }}-immortalwrt-${{ hashFiles(
            'build_cudy_immortalwrt.sh',
            '**/.config',
            '**/feeds.conf',
            '**/feeds.conf.default',
            '**/*.patch'
            ) }}
          restore-keys: |
            ${{ runner.os }}-immortalwrt-

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt install -y ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
            bzip2 ccache clang cmake cpio curl device-tree-compiler ecj fastjar flex gawk gettext gcc-multilib \
            g++-multilib git gnutls-dev gperf haveged help2man intltool lib32gcc-s1 libc6-dev-i386 libelf-dev \
            libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses-dev libpython3-dev \
            libreadline-dev libssl-dev libtool libyaml-dev libz-dev lld llvm lrzsz mkisofs msmtp nano \
            ninja-build p7zip p7zip-full patch pkgconf python3 python3-pip python3-ply python3-docutils \
            python3-pyelftools qemu-utils re2c rsync scons squashfs-tools subversion swig texinfo uglifyjs \
            upx-ucl unzip vim wget xmlto xxd zlib1g-dev zstd

      - name: Setup ccache
        run: |
          mkdir -p "$CCACHE_DIR"
          ccache -M 2G
          ccache --set-config=compression=true
          ccache --set-config=base_dir=${{ github.workspace }}
          ccache -s || true

      - name: Build firmware
        continue-on-error: true
        shell: bash
        run: |
          export PATH="/usr/lib/ccache:$PATH"
          bash ./build_cudy_immortalwrt.sh
          ccache -s || true
          ls -ahl

      - name: Build firmware retry
        if: failure() && !cancelled()
        env:
          NPROC: 1
        shell: bash
        run: |
          export PATH="/usr/lib/ccache:$PATH"
          bash ./build_cudy_immortalwrt.sh
          ccache -s || true
          ls -ahl

      - name: List file
        run: |
          ls -ahl .
          ls -ahl bin || true
          ls -ahl immortalwrt || true
          ls -ahl immortalwrt/bin/targets/mediatek/filogic || true

      - name: Upload bin artifacts
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt-bin
          path: immortalwrt/bin
          if-no-files-found: error

      - name: Upload sysupgrade bin
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt-sysupgrade-bin
          path: immortalwrt/bin/targets/mediatek/filogic/immortalwrt-mediatek-filogic-cudy_tr3000-v1-squashfs-sysupgrade.bin
          if-no-files-found: error
